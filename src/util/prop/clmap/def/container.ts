import type { PropClMap } from "#src/util/prop/clmap/type/prop.js"

export type PropClMap_DefContainer = PropClMap<keyof typeof prop_clmap_def_container>

export const prop_clmap_def_container = {
    __qyuddn: "__qyuddn",
    container: "container",
} as const
