import type { PropClMap } from "#src/util/prop/clmap/type/prop.js"

export type PropClMap_DefContent = PropClMap<keyof typeof prop_clmap_def_content>

export const prop_clmap_def_content = {
    __qyuddn: "__qyuddn",
    content: "content",
    _tracking: "_tracking",
    focusguard: "focusguard",
} as const
